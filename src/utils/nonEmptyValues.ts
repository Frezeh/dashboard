import { SortProps } from "../types";

function nonEmptyValues (values: SortProps) {
        for (const [key, value] of Object.entries(values)) {
            if(value === "") {
                
            }
        }      
}